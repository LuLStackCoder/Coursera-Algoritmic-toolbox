package main

import (
	"fmt"
)

func maxPairwiseProduct(numbers []int) int {
	n := len(numbers)
    i_max1 := 1
	i_max2 := 0
	for i := 2; i < n; i++ {
		
	}
	return
}

func main() {
	var n int
	fmt.Scanln(&n)
	numbers := make([]int, n)
	for i := 0; i < n; i++ {
		fmt.Scanln(numbers[i])
	}
}
